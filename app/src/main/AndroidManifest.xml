<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sdl.knowlegdetrove">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KnowlegdeTrove">
        <activity android:name=".AllUploadedebooks"></activity>
        <activity android:name=".Uploadpdf" />
        <activity android:name=".Ebooks" />
        <activity android:name=".Listofalluseruploadedbooks" />
        <activity android:name=".BuyBooks" />
        <activity android:name=".AddaNewBook" />
        <activity android:name=".Sellbooks1" />
        <activity android:name=".Categories" />
        <activity android:name=".Signup" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>